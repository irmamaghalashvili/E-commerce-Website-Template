import { H2styleDirectiveDirective } from './h2style-directive.directive';

describe('H2styleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new H2styleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
